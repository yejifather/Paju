package com.tenkitchen.classes

import android.util.Log

class ItemModel(
    var regDate: String,
    var memHp: String,
    var proPrice: String,
    var setNum: Int,
    var proName: String,
    var buyCount: Int ) {

    // 기본 생성자
    init {

    }

}